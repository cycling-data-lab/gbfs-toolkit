# API reference

Every public function is also available as a `.gbfs` accessor method and is pure
(so `df.pipe(...)` works). Optional features live behind extras (`[fetch]`, `[geo]`,
`[parquet]`, `[cluster]`).

::: gbfs_toolkit
